#include <iostream>
// #include "demochessboard.cpp"
// #include "demochesspiece.cpp"

using namespace std;

/*int main()
{
    Board board;
    board.printBoard();

    bool gameOver = false;
    while (!gameOver)
    {
        // Input player's move
            // Click on piece (Click on other places -> do nothing)
            // Show valid moves of that piece
            // Click on desired move
        // Update board
            // Update pieces' position
            // Print board
        // Check if over
        // no -> Switch turn 
        // yes
            // !King.canMove() && check -> Checkmate -> Win
            // !King.canMove() && !check -> Stalemate -> Draw
            // moveWithoutCheckOrPawnMove = 50 -> Draw
                // (?) quick_Draw when Insufficent Material
        // ->
        if (WhiteWins || BlackWins || Draw)
        {
            gameOver = true;
        }
    }
}*/

int main()
{
    cout << "Hello World!";
    return 0;
}